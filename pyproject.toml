[project]
name = "floravoice-fastapi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14.3"
dependencies = [
    "fastapi[standard]>=0.135.1",
]

[dependency-groups]
dev = [
    "ruff>=0.15.6",
]

[tool.fastapi]
entrypoint = "app.main:app"